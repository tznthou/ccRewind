appId: com.ccrewind.app
productName: ccRewind
directories:
  buildResources: build
  output: dist
files:
  - out/**/*
afterPack: ./scripts/afterPack.js
mac:
  target:
    - dmg
    - zip
  artifactName: ${name}-${version}-${arch}.${ext}
npmRebuild: true
asarUnpack:
  - node_modules/better-sqlite3
